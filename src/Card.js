import React from 'react';
import GetData from './GetData.js'

const Card = ({name, model, manufacturer}) => {
    return name, model, manufacturer
    console.log(name, model, manufacturer)
}
export default Card 